export default class Constants {
    //add sliders
    static G = 6.67e-11;
    static k = 8.99e9
    static KTimesProtonChargeSquared = this.k*Math.pow(1.6e-19, 2)
    // static KTimesProtonChargeSquared = 10*Math.pow(1.6e-19, 2)
    // static distanceScale = Math.pow(10,-10);
    static distanceScale = 1;
}